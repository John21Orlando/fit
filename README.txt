CutLog — local-first

1) 用 VSCode 安装扩展：Live Server
2) 右键 index.html -> Open with Live Server
3) 浏览器中使用；可“安装到桌面/主屏幕”

数据默认只在浏览器本地（IndexedDB）
备份在“备份”页导出 JSON


v3 变更：饮食只保留一个“结果”模块；文字输入自动估算范围；去掉外部估算/自动合并/文字估算按钮。


v4：加入「每日目标 kcal」与「预算口径（中值/上限）」；汇总页显示摄入范围与剩余预算。
